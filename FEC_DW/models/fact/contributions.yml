version: 2
models:
  - name: contributions
    description: Individual contribution fact. One row per transaction.
    columns:
      - name: contributionkey
        description: The surrogate key of the transaction
        tests:
          - not_null
          - unique
      - name: transactionid
        description: The natural key of the transaction
        tests:
          - not_null
          - unique