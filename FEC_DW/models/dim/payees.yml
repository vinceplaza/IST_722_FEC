version: 2
models:
  - name: payees
    description: Payees dimension. One row per payee
    columns:
      - name: payeekey
        description: The primary surrogate key of the payee
        tests:
          - not_null
          - unique
      # - name: payeekey
      #   description: The natural key of the payee
      #   tests:
      #     - not_null
      #     - unique
